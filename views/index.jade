extends layout

block content
	h1= title
	p Welcome to #{title}
	script(src='/socket.io/socket.io.js')
	script(type='text/javascript')
		var sockets = io.connect()
		sockets.on('news', function(msg){
			console.log(msg)
			});